import setuptools

setuptools.setup(name="deep_nmmo",
                 version="1.0",
                 description="Deep learning library for the Neural MMO game.",
                 packages=setuptools.find_packages(),
                 python_requires='>=3.9.0')
